# web (Vite) — copy to .env.local and fill in. NEVER commit real values.
# Clerk publishable key (safe for the browser; still keep out of git).
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxx
# Base URL of the Railway API.
VITE_API_BASE_URL=https://sjtracker-production.up.railway.app
